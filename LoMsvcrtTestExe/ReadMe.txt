This is just a standard console application as generated by the Visual Studio wizard.

We then go so far as to ensure that we are linking the project with the static version of the C++ library.

Then we link to LoMsvcrt.lib and everything is viola... fixed up to use the MSVCRT.DLL when possible.

We turn off security checks to ensure a smaller build.

